<%= form_with(model: @category, class: 'mt-10 ml-12 flex flex-col gap-5 ms-4 justify-center content-center  flex-grow') do |form| %>
  <% if @category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% @category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class= "align-middle ">
    <%= form.label :name, style: "display: block" , class: "font-bold" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :icon, style: "display: block", class: "font-bold" %>
    <%= form.file_field :icon, id: 'icon_upload' %>
  </div>

  <div>
    <%= form.submit "Save", class: 'p-3 px-5 green-btn rounded hover:bg-green-600' %>
  </div>
<% end %>
